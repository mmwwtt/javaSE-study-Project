关键字
	static(静态)
		只能在类主体中定义，不能在方法中定义
		不需要实例对象就能访问
		不能直接访问/调用 非静态方法/变量
		所有实例对象共享，且只在类初次加载时被初始化，先于mian主方法执行
	synchronized(锁)
		可见性和原子性
		
	volatile(有序性，可见性) 
		比synchronized更轻量的同步机制,每次更新其他线程都立即可见
		
	serialize(序列化)
		将java对象转化为二进制字节流，便于数据存储和传输
		场景：持久化，将数据存入数据库；网络，进程之间传输
