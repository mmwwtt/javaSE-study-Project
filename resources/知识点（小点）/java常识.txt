

public类的类名要与所在文件名一致

.java文件编译 产生 .class文件(二进制字节码文件），字节码文件可以运行在不同平台所对应的JVM上


java中使用unicode编码,一个汉字用一个char表示
java中char是两个字节，最大2^16
面向对象的设计思路
	is-a:继承关系
	has-a:从属关系
	like-a:组合关系
多线程
	原子性（前三个都要先读取后操作，x=1直接赋值，所以只有最后一个是原子性的）
		x++ 、++x 、x=y 、x=1

Threaad的sleep()不会释放锁,调用wait()方法会释放锁 
一个线程调用yield方法，可以使具有相同优先级线程获得处理器
在Java中，高优先级的可运行的线程会抢占低优先级线程的资源